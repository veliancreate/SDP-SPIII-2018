package flyweight

object PlatformFactory {

    private var map: MutableMap<String, Platform> = TODO()

    fun getPlatformInstance(platformType: String): Platform {
        TODO()
    } // this method will probably need to be synchronized
}
